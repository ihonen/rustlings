#!/usr/bin/env sh
set -eu

cd "$(dirname "$0")"

{
    echo "# Auto-generated by '$(basename "$0")'."
    echo DC_WORKSPACE=/workspaces/rustlings
    echo DC_USER="$(whoami)"
    echo DC_UID="$(id -u)"
    echo DC_GID="$(id -g)"
} > .env
